{% set name = "PyMca5" %}
{% set version = "5.7.4" %}

package:
  name: pymca
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: e2a93ae724e264b29d12a308664f86f9b36d8e99ceb0a3d5707ab0e03b80d313

build:
  number: 0
  script: '{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv '

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - fisx >=1.1.6
    - h5py
    - numpy
    - pip
    - python
    - cython
  run:
    - fisx >=1.1.6
    - h5py
    - matplotlib-base >1.0
    - {{ pin_compatible('numpy') }}
    - python

test:
  imports:
    - PyMca5
  commands:
    - pip check
  requires:
    - pip

about:
  home: http://pymca.sourceforge.net
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Mapping and X-Ray Fluorescence Analysis
  doc_url: http://www.silx.org/doc/PyMca/latest/tutorials.html

extra:
  recipe-maintainers:
    - tschoonj
